using UnityEngine;

public class GameManagersRoot : MonoBehaviour
{
    private static GameManagersRoot instance;

    void Awake()
    {
        if (instance != null && instance != this)
        {
            Destroy(gameObject);
            return;
        }
        instance = this;
        DontDestroyOnLoad(gameObject);
    }
}
